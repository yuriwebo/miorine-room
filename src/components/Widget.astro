---
const { title, imgSrc, imgAlt, itemName } = Astro.props;
---
<a href={`/${itemName}/`}>
  <section class={`${itemName}-container`}>
    <img src={imgSrc} alt={imgAlt} />
    <h2>{title}</h2>
  </section>
</a>
